"""Regression-testing primitives (Diff types, comparison harness, thresholds).

Importable from both the test suite (`tests/regression/`) and the `scout
diff-report` CLI. Pure logic — no I/O, no network, no LLM.
"""
